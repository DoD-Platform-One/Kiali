istio:
  enabled: true
  hardened:
    enabled: true
    monitoring:
      enabled: true

bbtests:
  enabled: true
  cypress:
    resources:
      requests:
        cpu: "2"
        memory: "1500M"
      limits:
        cpu: "2"
        memory: "1500M"

networkPolicies:
  enabled: true
  controlPlaneCidr: 172.16.0.0/12

resources:
  limits:
    cpu: "1"
